query(
  $route:String!
  $sizes:String!
  $colors:String!
  $effects:String!
  $query:String!
){
  filters(route:$route sizes:$sizes colors:$colors effects:$effects query:$query){
    title
    name
    items
  }
}
