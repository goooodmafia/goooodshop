query(
  $limit:Int!
  $offset:Int!
  $route:String!
  $languageCode:LanguageCodeEnum!
  $colors:String!
  $effects:String!
){
  fetchproducts(route:$route limit:$limit offset:$offset colors:$colors effects:$effects){
    model
    sku
    slug
    thumbnail{
      link
    }
    colors
    new
    hit
    sale
    glowInTheDark
    glowInTheUv
    description(languageCode:$languageCode)
    content(languageCode:$languageCode)
  }
}
