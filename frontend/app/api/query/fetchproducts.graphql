query(
  $limit:Int!
  $offset:Int!
  $route:String!
  $languageCode:LanguageCodeEnum!
  $colors:String!
  $effects:String!
  $tags:String!
){
  fetchproducts(route:$route limit:$limit offset:$offset colors:$colors effects:$effects tags:$tags){
    model
    sku
    slug
    thumbnail{
      link
    }
    colors
    category
    new
    hit
    sale
    glowInTheDark
    glowInTheUv
    description(languageCode:$languageCode)
    content(languageCode:$languageCode)
  }
}
