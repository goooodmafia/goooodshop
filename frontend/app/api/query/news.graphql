query (
  $pageSize:Int!
  $page:Int!
  $languageCode:LanguageCodeEnum!
){
  news(
    pageSize:$pageSize
    page:$page
  ){
    total
    hasNext
    items{
      title
      description(languageCode:$languageCode)
      image
      link
      slug
      viewDate
    }
  }
}
