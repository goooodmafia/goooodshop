query(
  $sku:String!
  $languageCode:LanguageCodeEnum!
){
  product(sku:$sku){
    sku
    model
    brand
    tags
    description(languageCode:$languageCode)
    content(languageCode:$languageCode)
    thumbnail
    mediaFiles
    new
    hit
    sale
    colors
    category
    glowInTheDark
    glowInTheUv
    slug
  }
}
